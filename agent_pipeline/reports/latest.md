# DFC Multi-Agent Audit Report

Status: pending runtime execution

This report is a bootstrap placeholder. Run the audit script after Python is available:

```bash
python agent_pipeline/run_agents.py
```

Current local environment issue:

```text
python command not found
py command not found
```

The multi-agent definitions are available in:

```text
agent_pipeline/agents.json
```

The workflow documentation is available in:

```text
docs/MULTI_AGENT_WORKFLOW.md
```
